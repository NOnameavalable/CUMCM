# P3 当前路径规划：python-tsp

`TaskPlanner.plan_route()` 直接替代原最近邻贪心选择器。依赖
`python-tsp==0.5.0` 已写入 `environment.yml`。

- 收集当前有效任务，按坐标精度合并位置；初始140个任务只有7个位置。
- 从机器狗当前位置出发，用位置距离除以速度构建移动时间矩阵。
- 将返回起点的费用设为零，求开放路线，不要求回原点。
- 使用 python-tsp 的 `two_opt` 局部搜索，以上次仍有效的路线和反向顺序
  分别启动，共享默认0.1秒搜索预算。新增位置进入初始路线，删除位置不再保留。
- 当前位置的任务先执行。同位置先清除、再优先检测当前频道；清除不切频道。
- 同一点构成一个访问批次。每次动作反馈后更新频道状态，直接从最新任务池
  选择该点下一任务，不运行TSP；新产生的原地清除可立即插入。该点任务耗尽
  后才规划下一位置。每个动作仍单独检查时间、动作上限、失效任务和退出条件。

目前TSP优化的是移动时间，不是完整的移动加切频道联合最优。
切频道依赖上一次测量，可能跨越多个清除动作，不能直接用相邻任务频道构造
普通距离矩阵。模拟器仍统计全部实际移动、切频道、测量和清除时间。

这是当前已知任务的近似规划，不保证全局最优。未来P1/P2点尚未生成，搜索
任务也可能因发现目标而取消，因此静态路线长度不等于最终执行总长度。
相同输入的局部搜索可能因库内部随机邻域顺序产生不同路线。

`--route-search-time 0.1` 可调整每轮搜索预算（秒）。不带 `--run` 仅做
离线检查；带 `--run` 时沿用原模拟器参数和接口。

验证命令：`python -m pytest test_p3.py -q`。
