# 图表生成与核验

本次修订仅处理摘要/实验删减和图文问题，不修改 P1—P4 算法实现。

## Python 图表

在仓库根目录，使用安装了项目依赖和 Matplotlib 的 Python 运行：

```bash
python paper/generate_review_figures.py
```

脚本更新图 6-2、7-2、7-3、8-1、8-2、8-3 的 PNG/PDF，并将计算出的半径、面积、路线长度、后验权重和探针坐标保存到 `figure_review_values.json`。

- 图 6-2 直接调用 P2 的区域、网格、状态更新函数，未重新求解候选点优化。
- 图 7-2(a) 直接读取七站生成函数；(b) 是固定节点的路线示意，不能替代演练任务快照。
- 图 7-3 直接调用带 5 米近场排除和 1500 米接收上限的 P1 几何计算，并区分新圆心和原待清除圆心。
- 图 8-1、8-2 的后验和探针通过当前混合状态与规划函数计算，输入为图注给出的固定观测示例。
- 图 8-3 的几何输入见表 5-1；失败扣除面板是独立构造示例。
- 图 6-1 图片无需修改：色条本身正确，本次纠正正文的颜色解释。

请勿用 `p2/generate_figures.py` 或 `p3/generate_figures.py` 的历史绘图输出覆盖上述修订图；其部分图例、节点和输出路径为旧版固定值。

## 问题一 TikZ 图

`problem1/figures/hull.tex` 和 `coverage.tex` 为单图编译入口，分别引用原有同名 TikZ 源码。用 XeLaTeX 编译后输出裁切的 PDF，再渲染 PNG 供 Markdown 嵌入。问题一原 LaTeX 正文仍直接引用 TikZ 源码。

## 范围

本次未运行正式评测或重新进行整套演练。三问的时间分解段、四问的接管前后提升对照、探针成功率及摘要末尾未附结果的验证声明已按要求删除。原演练结果文件保留。四问正式表因删除中间对照表，从表 8-4 顺延为表 8-3。
