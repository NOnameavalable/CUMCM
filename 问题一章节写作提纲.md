# 5 问题一：交会定位区域的构建与直径计算

> 文档用途：本文件是问题一论文正文的分层写作任务书。后续 agent 应依据本文件展开正式论述、生成图表并填入计算结果，不应擅自改变当前程序实际采用的算法。

## 本章总体目标

本章需要完成以下论证链条：

1. 将每次带有 $\pm1^\circ$ 误差的示向度转化为平面扇区约束；
2. 对目标圆域和全部测向扇区求交，得到干扰源定位可行区域 $\Omega$；
3. 对可行区域求凸包，并枚举凸包顶点对得到区域直径 $D$；
4. 以一组直径端点为直径构造圆，检验该圆能否覆盖整个定位区域；
5. 通过算例、图形和表格展示算法输出，并给出“直径圆不一定覆盖定位区域”的结论。

### 本章符号

| 符号 | 含义 | 单位 |
|---|---|---|
| $S_i=(x_i,y_i)$ | 第 $i$ 个检测点 | m |
| $\theta_i$ | 第 $i$ 个检测点测得的示向度 | $^\circ$ |
| $\varepsilon$ | 示向误差上界，本文取 $1^\circ$ | $^\circ$ |
| $W_i$ | 第 $i$ 次测向对应的扇形可行区域 | — |
| $B_0$ | 半径为 $1800\,\mathrm m$ 的目标圆域 | — |
| $\Omega$ | 多次观测的公共定位可行区域 | — |
| $H$ | $\Omega$ 的凸包 | — |
| $A,B$ | 定位区域的一组直径端点 | — |
| $D$ | 定位区域直径，即区域内两点的最大距离 | m |
| $C$ | 直径圆圆心，$C=(A+B)/2$ | — |
| $\tau$ | 浮点计算容差 | m |

---

## 5.1 示向误差几何模型

### 本节要回答的问题

说明为什么一次示向测量对应一个扇区，而不是一条精确射线，并将题目给出的误差范围转化为可计算的几何约束。

### 正文需要讲述的内容

1. 建立全局直角坐标系：目标圆域中心为原点，$x$ 轴正向向东，$y$ 轴正向向北。
2. 设检测点为 $S_i=(x_i,y_i)$，示向机读数为 $\theta_i$。
3. 题目规定测向误差位于 $[-1^\circ,1^\circ]$，因此真实方位角 $\varphi_i$ 满足

   $$
   \theta_i-\varepsilon\leq \varphi_i\leq\theta_i+\varepsilon,
   \qquad \varepsilon=1^\circ.
   $$

4. 定义两条边界射线的单位方向向量：

   $$
   \boldsymbol u_i^-=
   \begin{pmatrix}
   \cos(\theta_i-\varepsilon)\\
   \sin(\theta_i-\varepsilon)
   \end{pmatrix},
   \qquad
   \boldsymbol u_i^+=
   \begin{pmatrix}
   \cos(\theta_i+\varepsilon)\\
   \sin(\theta_i+\varepsilon)
   \end{pmatrix}.
   $$

5. 以 $S_i$ 为顶点、以上述两条射线为边界，得到角宽为 $2\varepsilon$ 的测向可行扇区 $W_i$。
6. 可以给出便于程序判定的叉积表达式：

   $$
   W_i=\left\{P\,\middle|\,
   \boldsymbol u_i^-\times(P-S_i)\geq0,
   \quad
   \boldsymbol u_i^+\times(P-S_i)\leq0
   \right\}.
   $$

7. 说明同一地点的测量误差在一段时间内保持不变，因此不采用“同一点重复测量取平均”的处理；不同位置的测向信息通过几何交会进行融合。
8. 当前问题一程序默认只使用测向角和目标区域约束，不使用有效接收半径及 $5\,\mathrm m$ 近场盲区。正文需要明确这一建模口径，避免公式和程序不一致。

### 本节需要放置的图

**图5-1：单点示向误差扇区示意图（建议，亦可与图5-2合并）**

- 画出一个检测点 $S_i$；
- 从 $S_i$ 出发画一条橙色实线箭头，标记为示向度中心线 $\theta_i$；
- 在中心线两侧各画一条橙色虚线，分别标记 $\theta_i-1^\circ$ 和 $\theta_i+1^\circ$；
- 两条虚线之间用浅橙色半透明区域填充并标记为 $W_i$；
- 画出从 $x$ 轴正方向逆时针旋转到中心线的方位角弧；
- 图注建议：“单次示向测量的区间几何模型”；
- 必须注明：若为增强可读性而放大图中的误差角，需要在图注中写“角度经视觉放大，计算取 $1^\circ$”。若按真实角度绘制则无需该说明。

### 代码依据

- `P1.py::_make_detection_sector`
- `utils.py::DetectionSector.from_measurement`
- `utils.py::DetectionSector.contains`

### 禁止误写

- 不得把示向度直接当作无误差射线；
- 不得宣称同一点重复测量能够产生独立误差并通过平均消除；
- 不得在本节加入当前问题一默认程序未采用的概率分布假设。

---

## 5.2 交会定位区域构建

### 本节要回答的问题

说明如何融合多个检测点的测向信息，并构造满足全部观测和目标区域限制的公共定位区域。

### 正文需要讲述的内容

1. 目标分布区域为

   $$
   B_0=\left\{(x,y):x^2+y^2\leq1800^2\right\}.
   $$

2. 同一干扰源必须同时满足全部示向约束，因此最终定位可行区域为

   $$
   \boxed{\Omega=B_0\cap\bigcap_{i=1}^{m}W_i}.
   $$

3. 描述程序的增量求交过程：首先令 $\Omega_0=B_0$，随后依次计算

   $$
   \Omega_i=\Omega_{i-1}\cap W_i,
   \qquad i=1,2,\ldots,m.
   $$

4. 每增加一个有效观测，定位区域不会扩大，即

   $$
   \Omega_i\subseteq\Omega_{i-1}.
   $$

5. 若某一步交集为空或面积小于数值阈值，说明观测数据彼此不相容、定位区域退化，程序停止并报告异常。
6. 当前实现使用 Shapely 完成几何布尔求交。目标圆以每象限64段的折线近似；扇区外边界默认使用65个圆弧采样点。因此程序计算的是连续区域的高精度多边形近似。
7. 解释扇区外边界仅用于把无限扇区截断为有限几何对象。默认截断距离取

   $$
   L_i=\|S_i-O\|+1800+1,
   $$

   该距离超过检测点到目标圆域内任一点的最大可能距离，因而不会剪掉目标圆域内的可行点。

### 本节需要放置的图

**图5-2：多点示向误差扇区及交会定位区域（必须）**

- 画出半径为 $1800\,\mathrm m$ 的蓝色目标圆域；
- 圆内布置至少三个检测点 $S_1,S_2,S_3$；
- 每个检测点均画出示向度中心线和 $\theta_i\pm1^\circ$ 两条边界虚线；
- 将全部扇区与目标圆域的公共交集用红色半透明颜色填充，并标记为 $\Omega$；
- 因真实 $\pm1^\circ$ 交集在全域尺度下较小，增加一个局部放大窗，建议放大9倍；
- 图中需要包含东、北坐标方向和 $500\,\mathrm m$ 比例尺；
- 图注建议：“多点示向误差扇区及交会定位区域示意图”；
- 现有可直接使用的源文件：`figures/p1_intersection.tex`；
- 现有已编译矢量图：`figures/build/p1_intersection.pdf`。

### 建议补充的算法框

```text
输入：检测数据 {(xi, yi, θi)}，误差 ε=1°，目标半径 R=1800 m
Ω ← B((0,0), R)
for i = 1,...,m:
    Wi ← 由 Si、θi−ε、θi+ε 构造的测向扇区
    Ω ← Ω ∩ Wi
    if Ω 为空或退化:
        报告观测数据不相容
输出：定位可行区域 Ω
```

### 代码依据

- `P1.py::solve_problem_1_geometry` 第97—110行附近
- `utils.py::DetectionSector.to_region`
- `utils.py::Region.disk`
- `utils.py::Region.intersection`

### 禁止误写

- 不得笼统写成“多条射线求交”；实际求交对象是具有误差宽度的扇区；
- 不得称程序使用解析圆弧进行完全精确运算；实际采用高精度折线离散；
- 若正文称 $\Omega$ 为“多边形”，应补充说明目标圆弧在数值实现中已经多边形化。

---

## 5.3 定位区域直径计算

### 本节要回答的问题

给出定位区域直径的数学定义，说明为什么只需考察凸包顶点，并准确描述当前代码的最远点对算法。

### 正文需要讲述的内容

1. 定义定位区域直径：

   $$
   D=\operatorname{diam}(\Omega)
   =\max_{P,Q\in\Omega}\|P-Q\|_2.
   $$

2. 对区域 $\Omega$ 求凸包

   $$
   H=\operatorname{Conv}(\Omega).
   $$

3. 说明区域直径与其凸包直径相同；凸包内部点不可能比凸包边界上的极点形成更大的距离，因此最远点对可以在凸包顶点中取得。
4. 设凸包顶点集合为 $V(H)=\{P_1,P_2,\ldots,P_h\}$，程序枚举全部顶点对：

   $$
   (A,B)=\arg\max_{1\leq i<j\leq h}\|P_i-P_j\|_2,
   \qquad D=\|A-B\|_2.
   $$

5. 当前代码采用“两两枚举”，而不是旋转卡壳：
   - 求凸包后需要计算 $h(h-1)/2$ 个顶点对；
   - 直径计算时间复杂度为 $O(h^2)$；
   - 额外空间复杂度为 $O(h)$；
   - 本题的离散边界顶点数量有限，因此该方法实现简单且计算量可接受。
6. 如需提到改进方向，可在模型评价部分写：对于极高分辨率边界，可采用旋转卡壳把凸包直径搜索降至 $O(h)$；不得将其写成当前已经使用的算法。

### 本节需要放置的图

**图5-3：凸包及最远点对示意图（必须）**

- 画出一个不规则定位区域 $\Omega$，用浅色填充；
- 用较深轮廓线画出其凸包 $H$；
- 将凸包顶点以小圆点标出；
- 高亮距离最大的两个顶点 $A$ 和 $B$；
- 用粗实线连接 $A,B$，在线段旁标记 $D=\|A-B\|_2$；
- 可用浅灰色虚线示例两至三组非最大候选顶点对，体现“枚举比较”的过程；
- 图注建议：“定位区域凸包及最远点对示意图”；
- 图形只需表达算法原理，不应虚构具体数值。

### 建议放置的伪代码

```text
H ← ConvexHull(Ω)
D ← 0
for i = 1,...,h−1:
    for j = i+1,...,h:
        dij ← EuclideanDistance(Pi, Pj)
        if dij > D:
            D ← dij
            A, B ← Pi, Pj
return D, A, B
```

### 代码依据

- `utils.py::Region.convex_hull`
- `utils.py::region_diameter`
- `utils.py::Region.diameter_with_endpoints`

### 禁止误写

- 不得声称当前代码采用旋转卡壳；
- 不得只写“寻找最长边”，区域直径不一定是多边形的一条边；
- 不得把直径 $D$ 与定位区域面积混为同一种评价指标。

---

## 5.4 直径圆覆盖性分析

### 本节要回答的问题

判断以定位区域直径为圆的直径时，该圆是否一定能够覆盖整个定位区域，并给出程序可执行的判据。

### 正文需要讲述的内容

1. 由最远点对 $A,B$ 构造直径圆，其圆心和半径为

   $$
   C=\frac{A+B}{2},
   \qquad r=\frac{D}{2}.
   $$

2. 对定位区域的每个边界顶点 $P_k$ 检查

   $$
   \|P_k-C\|_2\leq\frac{D}{2}+\tau.
   $$

3. 若全部顶点均满足上式，则直径圆覆盖整个多边形区域；只要存在一个顶点不满足，就不能覆盖。
4. 给出等价的泰勒斯圆判据：

   $$
   (P_k-A)\cdot(P_k-B)\leq0.
   $$

5. 解释只检查顶点的合理性：点到固定圆心的距离函数在多边形上的最大值可以在顶点处取得。
6. 程序使用数值容差

   $$
   \tau=\max(10^{-9},10^{-10}D)
   $$

   以减少浮点舍入误差对边界点判定的影响。
7. 必须明确回答题目：直径圆并不一定能够覆盖整个定位区域。
8. 用等边三角形作为反例：设三角形边长为 $D$，其区域直径也是 $D$；以任意一条边为直径的圆半径为 $D/2$，第三个顶点到该边中点的距离为 $\sqrt3D/2>D/2$，因此第三个顶点位于圆外。
9. 可补充说明：程序返回的是针对当前具体定位区域的布尔判断，而不是始终返回“不能”。

### 本节需要放置的图

**图5-4：直径圆覆盖与不覆盖的对比图（必须）**

- 使用左右两个子图；
- 左图画一个可被直径圆覆盖的定位区域，例如矩形或钝角三角形；
- 标出直径端点 $A,B$、圆心 $C$、直径 $D$ 和半径 $D/2$；
- 整个区域应位于圆内，标题写“(a) 可以覆盖”；
- 右图使用等边三角形，选择一条边作为直径 $AB$；
- 用醒目的紫色或橙色标记落在圆外的第三个顶点 $P$，并标出
  $\|P-C\|=\sqrt3D/2>D/2$；
- 标题写“(b) 不能覆盖”；
- 图注建议：“定位区域直径圆覆盖性的两种情形”；
- 图中颜色应与图5-2保持一致，并保证黑白打印时可以通过线型区分。

### 代码依据

- `P1.py::_diameter_circle_covers`
- `P1.py::solve_problem_1_geometry` 中圆心和覆盖结果的计算
- `test_main.py::test_equilateral_triangle_is_not_covered_by_a_diameter_circle`

### 禁止误写

- 不得得出“任何区域的直径圆必然覆盖该区域”的结论；
- 不得把直径圆直接称为最小覆盖圆；二者一般不等价；
- 不得只检查最远点对 $A,B$，因为它们必然在圆周上，不能说明其余顶点被覆盖。

---

## 5.5 算例与结果分析

### 本节要回答的问题

通过一组可复现输入完整展示“扇区构造—区域求交—直径计算—覆盖判定”的输出，并说明结果的物理意义。

### 正文需要讲述的内容

1. 选择至少含三个不同检测位置的同一干扰源测向数据作为主算例。
2. 输入数据必须来自实际程序运行、演练日志或明确标注的构造算例，不得混用不同干扰源或不同频道的观测。
3. 按以下格式列出输入：

   | 检测点 | $x_i/\mathrm m$ | $y_i/\mathrm m$ | $\theta_i/(^\circ)$ | 误差范围 |
   |---|---:|---:|---:|---|
   | $S_1$ | 待填 | 待填 | 待填 | $\pm1^\circ$ |
   | $S_2$ | 待填 | 待填 | 待填 | $\pm1^\circ$ |
   | $S_3$ | 待填 | 待填 | 待填 | $\pm1^\circ$ |

4. 使用 `solve_problem_1_geometry()` 计算并记录：
   - 定位区域面积；
   - 定位区域边界或顶点数；
   - 直径 $D$；
   - 直径端点 $A,B$；
   - 直径圆圆心 $C$；
   - 覆盖判断结果。
5. 按以下格式展示输出：

   | 定位区域面积/\(\mathrm{m}^2\) | 直径 $D/\mathrm m$ | 端点 $A$ | 端点 $B$ | 圆心 $C$ | 是否覆盖 |
   |---:|---:|---|---|---|---|
   | 待填 | 待填 | 待填 | 待填 | 待填 | 是/否 |

6. 结果分析至少包括：
   - 多位置示向扇区交会后，候选区域相较单次观测明显缩小；
   - 直径 $D$ 表示定位结果在最不利方向上的最大空间不确定尺度；
   - 覆盖判断决定能否以直径中点作为圆心、以 $D/2$ 为半径覆盖全部候选位置；
   - 若要与后续清除半径 $20\,\mathrm m$ 联系，应检查的是覆盖圆半径是否不超过 $20\,\mathrm m$，不能只检查 $D\leq40\,\mathrm m$ 而忽略覆盖性。
7. 如篇幅允许，补充数值离散敏感性检查：改变目标圆离散精度，比较 $D$ 和面积的变化，以证明默认离散参数已经稳定。不得在未运行实验时填入虚构数据。

### 本节需要放置的图表

**表5-1：问题一主算例测向输入数据（必须）**

- 按检测顺序列出坐标和示向度；
- 小数位保持一致；
- 在表下注明数据来源和频道。

**图5-5：主算例定位结果图（必须）**

- 使用主算例真实坐标绘图，不使用纯示意坐标；
- 展示目标圆域、检测点、全部误差扇区和公共定位区域；
- 标出最远点 $A,B$、线段 $AB$、中点 $C$ 以及直径圆；
- 若直径圆不能覆盖区域，用另一颜色高亮圆外部分；
- 图中直接标注数值 $D=\text{待填}\,\mathrm m$；
- 图注建议：“问题一主算例的定位区域、区域直径及直径圆”。

**表5-2：问题一主算例计算结果（必须）**

- 展示面积、直径端点、圆心、直径和覆盖结论；
- 数值精度建议：坐标和距离保留两位小数，面积根据量级保留两位或使用科学计数法。

**表5-3：离散精度敏感性分析（可选）**

- 至少比较三组圆弧离散精度；
- 列出顶点数、面积、直径及相对变化率；
- 只有实际运行取得数据后才能加入正文。

### 代码与数据依据

- `P1.py::solve_problem_1_geometry`
- `P1.py::Problem1Geometry`
- 演练或正式测试日志中的同频道测向记录
- 生成结果图时应直接读取同一份输入数据，避免图表数值不一致

### 禁止误写

- 不得使用 `P1.py` 文件末尾的两点调用示例充当正式实验结论，除非明确标注其只是接口演示；
- 不得填入未由程序计算的面积、端点或覆盖结果；
- 不得只展示图而省略可复核的输入与数值输出。

---

## 5.6 问题一结论

### 本节需要讲述的内容

本节控制为一至两段，概括以下结论：

1. 本文将 $\pm1^\circ$ 示向误差转化为扇区约束，通过多个扇区与目标圆域的几何交集得到定位可行区域；
2. 对区域凸包顶点进行两两距离比较，获得定位区域直径及一组直径端点；
3. 以直径端点中点为圆心、$D/2$ 为半径，并检查全部区域顶点，实现直径圆覆盖性的数值判定；
4. 直径圆是否覆盖取决于定位区域的具体形状，不能仅根据直径数值作出肯定结论；
5. 该算法输出的定位区域、直径端点、圆心和覆盖结论可作为后续检测点选择及自动清除策略的几何基础。

### 本节是否需要图表

本节不新增图表，只引用图5-2、图5-4和主算例结果表。

### 推荐结论句式

> 综上，多点区间测向能够把单次观测形成的狭长扇区收缩为有限定位区域。区域直径刻画了定位结果的最大空间不确定性，但以该直径构造的圆并不必然覆盖整个区域，仍需结合全部边界顶点进行覆盖检验。

---

## 后续 agent 的直接执行清单

1. 阅读 `ProblemB/ProblemB.pdf` 中问题一及附件2的测向误差、坐标系和交会定位说明。
2. 阅读 `P1.py` 与 `utils.py` 中上述函数，保证论文算法与实现一致。
3. 保留并复核现有图5-2：`figures/p1_intersection.tex`。
4. 新建图5-3“凸包及最远点对示意图”的 TikZ 源码并编译为PDF。
5. 新建图5-4“直径圆覆盖与不覆盖对比图”的 TikZ 源码并编译为PDF。
6. 从同一频道的真实测向记录选取主算例；若没有可用日志，则先生成可复现构造算例并明确标注。
7. 调用 `solve_problem_1_geometry()` 导出表5-1、表5-2所需数值。
8. 使用同一组数据生成图5-5，核对图中端点、圆心和表格完全一致。
9. 如执行离散敏感性实验，生成表5-3；否则删除该可选表，不得留空表进入终稿。
10. 将各节扩写为正式论文文字，减少代码名和软件调用描述，突出数学模型、算法依据和结果解释。

## 验收标准

- 正文明确写出 $\Omega=B_0\cap\bigcap_i W_i$；
- 正文明确写出直径定义及直径端点搜索公式；
- 算法名称与代码一致：凸包顶点两两枚举，不写成旋转卡壳；
- 明确回答“直径圆不一定覆盖定位区域”；
- 至少包含交会定位图、凸包最远点对图、覆盖性对比图和主算例结果图；
- 所有实验数值均可由代码或日志复现；
- 图、表、正文使用相同符号和相同数据；
- 不把直径圆误称为最小覆盖圆；
- 不把数值多边形近似表述为完全解析圆弧计算。
