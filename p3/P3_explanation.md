# P3 框架原理与使用说明

本文配合 CUMCM 项目中的 P3.py 阅读，介绍运行方法、P1/P2/utils 的复用关系、搜索与定位原理以及当前框架的限制。当前版本已完成语法检查、覆盖布局抽样和合成双测向检查，尚未完成真实模拟器端到端验证。

## 1 运行方法

P1.py、P2.py、P3.py 和 utils.py 应位于同一目录。在 PyCharm 底部打开 Terminal，执行：

```powershell
cd C:\Users\13725\Desktop\study\CUMCM
.\.venv\Scripts\python.exe -m pip install numpy shapely
.\.venv\Scripts\python.exe -c "import P1, P2, P3; print('导入成功')"
```

使用项目解释器安装依赖，可以避免安装到另一个 Python 环境。当前 P3 导入链需要 NumPy 和 Shapely；通信使用标准库 urllib。运行 P2 绘图时还需安装 matplotlib。

### 离线检查

右键 P3.py 选择运行，或者执行：

```powershell
.\.venv\Scripts\python.exe P3.py
```

程序输出七个扫描点、最大抽样覆盖距离约900米、coverage_guaranteed 为 true，以及基础路线与时间估算。该模式不会连接模拟器，也没有执行完整目标定位清除。

### 练习运行

在模拟器中登录，启动第三问全向源练习，等待接口就绪，再执行：

```powershell
.\.venv\Scripts\python.exe P3.py --run --robot-id 你的参赛队号 --log practice_01.jsonl
```

用实际队号替换“你的参赛队号”。默认连接 http://127.0.0.1:2026；端口不同时添加 `--base-url http://127.0.0.1:2030` 等参数。PyCharm 运行配置也可填写相同的脚本参数，工作目录设为 CUMCM。

| 参数 | 用途 | 默认行为 |
|---|---|---|
| --run | 连接模拟器执行 | 未指定时只离线检查 |
| --robot-id | 当前登录的参赛队号 | 联网运行必填 |
| --base-url | 本机模拟器地址 | http://127.0.0.1:2026 |
| --log | 调试日志位置 | P3_run_log.jsonl |

日志相对于工作目录保存，采用追加写入，每次练习建议使用不同文件名。

## 2 第三问的目标

半径1800米的目标圆内有10至16个全向源，占用20个频道中的不同频道。各源接收半径在1000至1500米之间，具体位置与半径未知。机器人需要发现、定位和清除全部目标，并减少移动、测量、切换频道和清除的总虚拟时间。

一次 no_signal 不能说明频道不存在，因为距离过远也会无信号。清除10个目标后也不能停止，因为本局可能存在更多目标。因此程序需要同时维护已发现目标的定位状态和未知频道的覆盖扫描进度。

## 3 如何复用 P1 P2 和 utils

| 来源 | 复用内容 | 作用 |
|---|---|---|
| utils | Point | 表示坐标，计算距离和方位 |
| P1 | solve_problem_1_geometry | 构造多条方位观测的连续定位区域，返回顶点、直径和直径圆 |
| P2 | Problem2Config、StateSample、Observation | 配置、联合状态和观测结构 |
| P2 | _build_first_feasible_region | 首次方位可行域 |
| P2 | _sample_target_cells、_expand_receive_radius_states | 位置单元和接收半径样本 |
| P2 | _state_arrays、_is_observation_compatible | 批量过滤状态 |
| P2 | _minimum_enclosing_circle | 样本最小包围圆 |
| P2 | solve_problem_2 | 推荐第二测量点 |

P1 已接入定位主流程。第一次方向观测后，P3 调用一次 `solve_problem_2()` 选择第二检测点；得到两次及以上方向观测后，`ChannelBelief.p1_geometry_result()` 调用 P1 构造连续交会定位区域，`p1_localization_circle()` 计算该区域的最小包围圆。该圆既用于 20 米清除判定，也用于选择第三次及后续检测点。P3 仍使用了 P2 下划线开头的内部函数，修改这些接口时需要同步检查 P3。

## 4 程序结构

```text
main
 ├─ 默认模式 → run_offline_check
 └─ --run → SimulatorClient + Problem3Controller
                              ├─ ChannelBelief × 20
                              ├─ CoveragePlanner
                              ├─ LocalizationPlanner
                              └─ GlobalScheduler → PlannedAction
                                                    measure / clear / exit
```

| 组件 | 职责 |
|---|---|
| Problem3Config | 物理参数、离散精度和运行保护 |
| ObservationRecord | 测量位置、类型、方位、时间、覆盖点编号 |
| ConservativeCircle | 圆心、样本半径及扩大后的半径 |
| ChannelBelief | 单频道观测历史、联合样本、发现和清除状态 |
| CoveragePlanner | 七点布局及未知频道扫描 |
| LocalizationPlanner | 推荐补测位置 |
| GlobalScheduler | 按优先级选择动作 |
| SimulatorClient | 请求构造、HTTP通信、幂等重试和日志 |
| Problem3Controller | 执行动作，更新状态，正常结束时输出摘要 |

## 5 七点覆盖原理

令 A = 1800，rho = A cos 30°，约1558.846米。原点加正六边形六个顶点构成扫描点：

| 编号 | x 米 | y 米 |
|---|---:|---:|
| 0 | 0 | 0 |
| 1 | 1558.846 | 0 |
| 2 | 779.423 | 1350 |
| 3 | -779.423 | 1350 |
| 4 | -1558.846 | 0 |
| 5 | -779.423 | -1350 |
| 6 | 779.423 | -1350 |

半径不超过900米的目标由原点覆盖。半径 r 在900至1800米之间时，选择角度差不超过30°的最近顶点，有：

```text
d² ≤ r² + rho² − 2 r rho cos 30°
```

右侧关于 r 是凸二次函数，在区间上的最大值位于端点。代入900和1800，两端均为900²。因此整个目标区域距至少一个扫描点不超过900米，小于最小接收半径1000米。

这是几何覆盖证明；verify_dense() 只是有限抽样复核。coverage_guaranteed 字段的代码计算本身属于抽样结果。

未知频道在七点逐一检测。一旦发现信号，就退出未知频道扫描队列，交由定位流程。全部七点都无信号才可判为空。所有频道都已清除或完成无信号覆盖时，程序正常退出。

固定路线从原点到首顶点，再走五条六边形边，长度为6 rho，约9353.07米；按5米每秒移动需1870.61秒。每点测20个频道需100秒加19秒切换，七点共833秒，基准总计2703.61秒。

该估算不含定位清除绕行。当前调度器会插入清除，因此它不是实际完整过程的严格时间上界。

## 6 联合后验与清除

后验是观测之后仍可能成立的状态。每个样本同时保存目标位置 p 和接收半径 R，并带有单元编号、权重和单元半径。

| 观测 | 连续模型含义 | 当前处理 |
|---|---|---|
| direction | 5 < 距离 ≤ R，且方位符合误差界 | 初始化或筛选样本 |
| no_signal | 对存在的未清除全向源，距离 > R | 未发现时记历史，已发现时筛选 |
| near | 距离 ≤ 5米 | 记录当前位置，优先清除 |

首次方向观测之前只记录无信号历史；首次方向观测后建立联合样本，再重放历史筛选。后验为空时，程序会依次使用原网格二分之一和四分之一的间距重建。如果两次以上示向度已由 P1 形成非空连续定位区域，离散重建仍为空只表示网格漏掉了狭小交集，此时程序改由 P1 连续区域接管，不再错误终止；P1 也无法形成区域时才报错。

P2 相容函数加入分箱半宽，默认方向容差为1° + 0.25° / 2，即1.125°。这是离散计算设置，不是题目规定的物理误差。

程序对位置代表点求最小包围圆，并加上最大单元半径。估计半径不超过20米时前往圆心清除；near 时在当前点清除。

方向和接收半径观测仍主要依据单元代表点过滤，接收半径也只有有限样本，可能提前丢弃与观测相容的真实状态。清除失败约束采用“代表点距离 + 单元半径”的保守判定，避免删除一部分仍位于失败圆外的单元；后验坍缩时还会以二分之一、四分之一网格重放全部历史。然而，这仍不是严格的连续可行域运算，因此“保守圆”尚不是连续问题的无条件清除保证，最终应升级为整单元相容筛选或连续外包区域与接收半径区间。

P1 的直径不超过40米也不能单独保证20米圆覆盖，仍需检验覆盖圆。

## 7 实际调度顺序

1. 优先清除满足判据的频道，选择最近的清除圆心。
2. 还有未知频道时，继续覆盖扫描。
3. 未知频道搜索完成后，对已发现未清除频道补测，优先圆心较近者。
4. 所有频道满足完成条件时退出。

只有一次方向观测时调用 `solve_problem_2()`，用 P2 选择第二检测点。已有两次或更多方向观测时，不再重复调用 P2，而是调用 P1 更新连续交会定位区域：最小包围圆半径不超过 20 米时在圆心清除；否则直接前往该圆心进行第三次或后续检测。该圆心是定位区域的极小极大中心，比一般四边形的顶点平均值或对角线交点更适合作为“定位区域中间”。清除失败时例外，程序必须换点补测，避免在同一失败圆心循环。

## 8 通信 日志与成绩

四个接口为 /enter、/measure、/clear、/exit。移动由 measure 或 clear 的 position 给出。只有 measure 切换测向机频道，clear 不改变测向机频道。请求串行执行。

新动作使用新 request_id，网络重试复用原ID及请求体。HTTP错误和 accepted=false 会报错；连接错误等按配置重试。只有 accepted=true 才更新虚拟时钟。

控制器依据 enter 返回的 remaining_real_duration_s 设置截止，默认预留10秒，但仅在主循环开始检查，不能中断长时间 P2 求解。还需补充求解预算、虚拟时间上限与异常收尾。

正常退出摘要包含 cleared_count、cleared_channels、virtual_time_s、average_localize_clear_time_s、program_runtime_s 和 actions_executed。平均时间为总虚拟时间除以清除数量；数量为0时为空。真实耗时从发送 enter 前计量，动作数只计测量与清除。

接口不提供真实总源数，当前不计算清除比例。应结合模拟器成绩确定分母，不能用发现数量替代真实总数。

JSONL 每行保存已接受请求的 path、request、response 和 wall_time_s。当前未完整记录拒绝动作、网络异常和调度理由。本地日志不是题目要求导出的正式加密日志，不能替代它。

## 9 参数与常见问题

| 参数 | 默认值 | 影响 |
|---|---:|---|
| max_actions | 2000 | 动作上限 |
| request_timeout_s | 5秒 | 单请求等待 |
| request_retries | 3 | 最多尝试次数 |
| real_time_reserve_s | 10秒 | 现实时间余量 |
| p2.target_grid_spacing | 30米 | 位置离散精度 |
| p2.receive_radius_sample_count | 7 | 接收半径样本数 |
| p2.candidate_grid_spacing | 120米 | 候选点粗间距 |
| p2.refined_grid_spacing | 30米 | 候选点细间距 |

顶层和 p2 配置分别保存物理参数，修改共同参数应同步更新，目前未自动强制一致。细化网格会增加计算量，也不能单独解决代表点过滤的严格性问题。

- 缺少依赖：用项目 .venv 解释器安装，并确保运行时使用同一解释器。
- 连接失败：检查测试是否就绪、端口是否匹配或测试是否结束。
- 请求拒绝：核对登录队号、是否重复进入和当前测试状态。
- 后验为空：不代表目标不存在；保留历史，检查参数并细化或重建后验。
- 清除失败：程序永久保存“目标位于该点20米圆外”的约束，阻止同点再次清除，并强制横向移动60米补测；若筛选后验坍缩，则自动用更细网格重放全部历史观测。若仍报后验不相容，应继续缩小网格或改用连续可行域。
- 暂时无输出：主循环没有逐动作进度打印，可能在计算候选点或等待网络，可观察日志是否增长。

## 10 后续完善与阅读顺序

优先继续完善后验正确性：整单元方向相容筛选和接收半径区间。当前已加入清除失败约束、自动细化重放及失败后强制换点补测。随后增加时间预算、虚拟上限、异常摘要和日志；最后优化同点多频道测量与移动路径。先在练习案例验证完整清除，再比较耗时。

阅读代码建议从 main 和 Problem3Controller.run 开始，再读 GlobalScheduler.next_action、ChannelBelief.update、conservative_circle，最后看 CoveragePlanner、LocalizationPlanner 与 SimulatorClient。结合 P1.py、P2.py、utils.py 和 P2_explanation.md 阅读。

本文解释当前代码行为。题目原文与模拟器附件仍是物理规则及接口要求的依据。
